#include <stdlib.h>
#include <stdio.h>
#include <string.h>
struct _a_struct {
  int x;
  int numStrs;
  char ** strs;
};
typedef struct _a_struct a_struct;


int main(void) {
  const char * strs[] = {"abc",
			 "def",
			 NULL};
  a_struct * a;
  a_struct * b;
  int i = 0;
  //WRITE ME
  //WRITE ME
  //WRITE ME
  //WRITE ME
  //WRITE ME
  while (strs[i] != NULL) {
    //WRITE ME
    //WRITE ME
    //WRITE ME
    //WRITE ME
  }
  for (i = 0; i < a->numStrs; i++) {
    printf("%s\n", a->strs[i]);
    //WRITE ME
  }
  //WRITE ME
  //WRITE ME
  //WRITE ME
  //WRITE ME
  return EXIT_SUCCESS;
}
